"""lst=[]  #lista vazia # criacao de lista
for c in range(1, 6):
    peso=float(input('Peso da {}ª pessoa: '.format(c)))
    lst+=[peso]   #adc os valores de peso na lista
print('')
print('O Maior peso foi:', max(lst))  #maximo valor da lista
print('O Menor peso foi:', min(lst))  #minimo valor da lista"""

list=[]
for c in range(0, 5):
    k = int(input("Qual o seu peso ? "))
    list += [k]
print("O maior peso foi {}.".format(max(list)))
print("O menor peso foi {}.".format(min(list)))
